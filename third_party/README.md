# Optional third-party backends

Large upstream repositories are intentionally not vendored into Git. Pinned revisions, licenses, patches, and setup commands will be recorded here and automated under `scripts/`.


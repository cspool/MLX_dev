# BERT 完整原生物理路径：数值验收

已归档的报告、执行源码快照与实际输出见[结果包](../artifacts/tagged/bert-physical-full-publication-001/README.md)。包内不含权重、可执行文件或运行中的整模结果。

`/workspace/mlx-qa-runtime-yJ2sOw/artifacts/tagged/model-e2e/bert-qa-physical-full-002`
正常退出（exit 0），完整主要正确性及独立复核通过。此处是按源顺序执行的
`physical` 模式，不是并发 `paired`、Chipyard 或作者 hybrid 验收。

- 1,141 原始算子、3,046 降级步骤全部执行。
- 窗口：矩阵 1,083、向量 2,040、搬运 691、控制 24。
- 13,415,491,584 矩阵 MAC；8,064,152,859 物理请求全部排空，所有缓冲区释放。
- 实际答案 Zurich、Zurich、Oslo；六个 start/end logits 文件与完整原生微程序基线逐位一致。
- 相对框架最大绝对误差 9.5367431640625e-6，容差失败元素 0。
- 功能快路径、BLAS、Python/GPU 执行回退计数均为 0。

独立复核检查二进制、源码快照、输入与依赖库摘要，重新调用
`verify_physical_execution`，直接比较六个输出文件字节和报告摘要。
`comparison.json` SHA256：
`ecff2e7de1eb0068a3f1b31b77aacdc1a930136669da9f6aeda89869c05141f3`。

| 计数 | 结果 |
| --- | ---: |
| 设备组件周期 | 92,401,432,887 |
| 最终读回周期／标量请求 | 7,800／780 |
| 合计目标周期 | 92,401,440,687 |
| 宿主运行秒数 | 50,927.3491 |

宿主时间不是目标推理延迟。此模式窗口内仍保留原有并发，但不是完整并发图
基线；不含实际 Rocket CPU、系统初始化和 QA 宿主后处理的目标时序。
不能用此周期替代尚未完成的配对路径发布 MLX 两模型误差。正式比较仍须等待
独立并发数值运行和完整事件图＋读回的同输入、同配置、同边界验收。

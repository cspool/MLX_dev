# H162 protocol: final simulator-trend goal certificate

## Hypothesis

The goal document is complete under the user-directed scope when the frozen
core-performance certificate, frozen 6/6 numeric functional chain, bounded
context/memory stress evidence and a fresh whole-repository verification all
qualify simultaneously, without requiring RTL, power/area, full-figure coverage
or 10% numerical agreement to paper bars.

## Frozen completion units

- H154: five primary and three supporting core architectural claims, each over
  a qualified same-work baseline with matching direction and gain at least
  1.2x.
- H161: six required operator/block payloads, one dynamically linked complete
  block, all numerical boundaries within 1e-12, exact work/traffic and
  enabled/disabled timing identity.
- H109/H113: bounded contexts, RF/backpressure/event behavior and coupled
  DPU-memory bank/queue/overlap stress remain supported.
- Fresh repository verification: Ruff over scripts/src/tests and the complete
  pytest suite after adding this certificate test. Expected result is 416
  passed, zero failed and 17 known warnings.

## Acceptance gates

1. All four frozen inputs pass byte/hash/status/integrity qualification.
2. H154 retains 5/5 primary and 3/3 supporting claims.
3. Every H154 claim uses a same-work/qualified parent, matches direction and
   has minimum gain at least 1.2x.
4. H161 retains 6/6 payloads in the registered order and one complete dynamic
   block with 54 blocks/PEs, 13 tags and four live links.
5. H161 final and boundary maximum errors are at most 1e-12; all operations,
   bytes, events and routes conserve and enabled/disabled timing is identical.
6. H109 bounded-context/backpressure regressions remain supported.
7. H113 coupled memory/bank/queue/non-stop-flow regressions remain supported.
8. Fresh Ruff returns zero with `All checks passed`.
9. Fresh full pytest returns zero with exactly 416 passed, zero failed and 17
   warnings.
10. The goal text retains the same-work, >=1.2x, six-family, target-free and
    cross-experiment stopping rules.
11. The certificate explicitly excludes RTL, power/area, strict 10% and full
    figures from primary completion.
12. Source and verification consume no paper target/residual fitting input.

The immutable result will be
`artifacts/results/simulator-trend-goal-completion-run167.json`.

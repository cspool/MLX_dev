# 本地模板编程组件证据

发布内容为正式报告、224项回归XML，以及9个计时、日志开关和多PE用例的完整程序/配置/结果与输出。ASan/UBSan/LSan重放的摘要由报告绑定，重复工作集及构建二进制保留本地。

该实现只计时已解码描述符到PE本地模板存储的逐字写入、可见性与发射竞争，不代表主机、cache或RoCC描述符传输已经验证。模板装载完成不等于完整模型、系统性能或RTL通过。

完整Llama2仍只有6181节点/873组程序的1周期限额启动记录，预期退出1且没有完整推理输出。未上传模型权重。

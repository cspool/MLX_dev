diff --git a/src/drt/src/TritonRoute.cpp b/src/drt/src/TritonRoute.cpp
index 6c636a27f..45eef1eea 100644
--- a/src/drt/src/TritonRoute.cpp
+++ b/src/drt/src/TritonRoute.cpp
@@ -30,8 +30,11 @@
 
 #include <boost/asio/post.hpp>
 #include <boost/bind/bind.hpp>
+#include <cstdint>
+#include <cstdlib>
 #include <fstream>
 #include <iostream>
+#include <map>
 
 #include "DesignCallBack.h"
 #include "db/tech/frTechObject.h"
@@ -961,6 +964,35 @@ int TritonRoute::main()
     }
   }
   initDesign();
+  if (std::getenv("MLX_DRT_STOP_AFTER_IMPORT") != nullptr) {
+    std::map<frLayerNum, uint64_t> layer_lengths;
+    uint64_t total_length = 0;
+    uint64_t total_vias = 0;
+    for (const auto& net : getDesign()->getTopBlock()->getNets()) {
+      for (const auto& shape : net->getShapes()) {
+        if (shape->typeId() != frcPathSeg) {
+          continue;
+        }
+        const auto* path_seg = static_cast<const frPathSeg*>(shape.get());
+        const auto [begin, end] = path_seg->getPoints();
+        const uint64_t length = std::abs(end.x() - begin.x())
+                                + std::abs(end.y() - begin.y());
+        layer_lengths[path_seg->getLayerNum()] += length;
+        total_length += length;
+      }
+      total_vias += net->getVias().size();
+    }
+    const double dbu = getDesign()->getTech()->getDBUPerUU();
+    for (const auto& [layer_num, length] : layer_lengths) {
+      logger_->report("MLX_DRT_IMPORT_WIRE layer={} length_um={}",
+                      getDesign()->getTech()->getLayer(layer_num)->getName(),
+                      static_cast<uint64_t>(length / dbu));
+    }
+    logger_->report("MLX_DRT_IMPORT_SUMMARY wirelength_um={} vias={}",
+                    static_cast<uint64_t>(total_length / dbu),
+                    total_vias);
+    return 0;
+  }
   bool has_routable_nets = false;
   for (auto net : db_->getChip()->getBlock()->getNets()) {
     if (net->getITerms().size() + net->getBTerms().size() > 1) {
diff --git a/src/drt/src/dr/FlexDR.cpp b/src/drt/src/dr/FlexDR.cpp
index 31b1f11b4..8afffd725 100644
--- a/src/drt/src/dr/FlexDR.cpp
+++ b/src/drt/src/dr/FlexDR.cpp
@@ -35,6 +35,7 @@
 #include <boost/archive/text_oarchive.hpp>
 #include <boost/io/ios_state.hpp>
 #include <chrono>
+#include <cstdlib>
 #include <cstdio>
 #include <fstream>
 #include <iomanip>
@@ -1272,9 +1273,23 @@ void FlexDR::searchRepair(const SearchRepairArgs& args)
   IterationProgress iter_prog;
   auto block = getDesign()->getTopBlock();
   const auto num_drvs = block->getNumMarkers();
-  const bool stubborn_flow = num_drvs <= 11 && ripupMode != RipUpMode::ALL
-                             && ripupMode != RipUpMode::INCR
-                             && !control_.fixing_max_spacing;
+  const char* stubborn_threshold_env
+      = std::getenv("MLX_DRT_STUBBORN_THRESHOLD");
+  int stubborn_threshold = 11;
+  if (stubborn_threshold_env != nullptr) {
+    const int requested_threshold = std::atoi(stubborn_threshold_env);
+    if (requested_threshold >= 11 && requested_threshold <= 64) {
+      stubborn_threshold = requested_threshold;
+    }
+  }
+  const bool stubborn_flow
+      = num_drvs <= stubborn_threshold && ripupMode != RipUpMode::ALL
+        && ripupMode != RipUpMode::INCR && !control_.fixing_max_spacing;
+  if (stubborn_flow && stubborn_threshold_env != nullptr) {
+    logger_->report("MLX_DRT_STUBBORN_THRESHOLD threshold={} violations={}",
+                    stubborn_threshold,
+                    num_drvs);
+  }
   if (router_cfg_->VERBOSE > 0) {
     printIteration(logger_, iter_, stubborn_flow);
   }
@@ -1844,11 +1859,18 @@ int FlexDR::main()
       break;
     }
   }
+  const bool skip_redundant_incremental
+      = std::getenv("MLX_DRT_SKIP_REDUNDANT_INCREMENTAL") != nullptr;
   for (auto& args :
        strategy(router_cfg_->ROUTESHAPECOST, router_cfg_->MARKERCOST)) {
     if (iter_ > router_cfg_->END_ITERATION) {
       break;
     }
+    if (skip_redundant_incremental && incremental && iter_ >= 1 && iter_ <= 2) {
+      logger_->report("MLX_DRT_SKIP_REDUNDANT_INCREMENTAL iteration={}", iter_);
+      ++iter_;
+      continue;
+    }
     int clipSize = args.size;
     if (args.ripupMode != RipUpMode::ALL) {
       if (increaseClipsize_) {
diff --git a/src/drt/src/io/io.cpp b/src/drt/src/io/io.cpp
index d2efa2d8d..88391d474 100644
--- a/src/drt/src/io/io.cpp
+++ b/src/drt/src/io/io.cpp
@@ -676,8 +676,6 @@ void io::Parser::updateNetRouting(frNet* netIn, odb::dbNet* net)
   frCoord endY = -1;
   frCoord endExt = -1;
   odb::dbTechLayer* prevLayer = nullptr;
-  odb::dbTechLayer* lower_layer = nullptr;
-  odb::dbTechLayer* top_layer = nullptr;
   bool hasRect = false;
   frCoord left = -1;
   frCoord bottom = -1;
@@ -762,10 +760,8 @@ void io::Parser::updateNetRouting(frNet* netIn, odb::dbNet* net)
             break;
           case odb::dbWireDecoder::VIA:
             viaName = std::string(decoder.getVia()->getName());
-            lower_layer = decoder.getVia()->getBottomLayer();
-            top_layer = decoder.getVia()->getTopLayer();
-            layerName = prevLayer == top_layer ? lower_layer->getName()
-                                               : top_layer->getName();
+            prevLayer = decoder.getLayer();
+            layerName = prevLayer->getName();
             if (!hasBeginPoint) {
               beginX = nextX;
               beginY = nextY;
@@ -775,10 +771,8 @@ void io::Parser::updateNetRouting(frNet* netIn, odb::dbNet* net)
             break;
           case odb::dbWireDecoder::TECH_VIA:
             viaName = std::string(decoder.getTechVia()->getName());
-            lower_layer = decoder.getTechVia()->getBottomLayer();
-            top_layer = decoder.getTechVia()->getTopLayer();
-            layerName = prevLayer == top_layer ? lower_layer->getName()
-                                               : top_layer->getName();
+            prevLayer = decoder.getLayer();
+            layerName = prevLayer->getName();
             if (!hasBeginPoint) {
               beginX = nextX;
               beginY = nextY;
@@ -800,9 +794,16 @@ void io::Parser::updateNetRouting(frNet* netIn, odb::dbNet* net)
         }
         pathId = decoder.next();
 
-        if (pathId == odb::dbWireDecoder::POINT && hasEndPoint) {
+        if ((pathId == odb::dbWireDecoder::POINT
+             || pathId == odb::dbWireDecoder::POINT_EXT)
+            && hasEndPoint) {
           frCoord x, y;
-          decoder.getPoint(x, y);
+          if (pathId == odb::dbWireDecoder::POINT_EXT) {
+            frCoord ext;
+            decoder.getPoint(x, y, ext);
+          } else {
+            decoder.getPoint(x, y);
+          }
           bool curr_conn_vertical = beginX == endX;
           bool next_conn_vertical = endX == x;
           orthogonal_conn = curr_conn_vertical != next_conn_vertical;
